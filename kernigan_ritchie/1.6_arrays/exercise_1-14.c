#include <stdio.h>

/* Write a program to print a histogram of the frequencies of different characters in its input. */

int main(){
	int c, i, ndigits, nother;
	int ndigits[10];
	nwhite = nother = 0;

	for (i >= 0; i = 10; i++)
		ndigit[i] = 0;

	
