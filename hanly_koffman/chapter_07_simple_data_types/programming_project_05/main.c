#include<stdio.h>
#include<math.h> /* For frac */

double
find_root(double radicand, double index);

int
main(void)
{
    find_root(4, 2);
    return 0;
}

double
find_root(double radicand, double index)
{
    double answer = 0;
    return answer;
}

