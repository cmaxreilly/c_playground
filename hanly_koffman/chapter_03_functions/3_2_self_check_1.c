/*
 * Rewrite the following expresions using C functions:
 */

#include <stdio.h>

void a(void);

int main (void) {
    a();
    return (0);
}


/* a. √(u + v) * w **2 */
void a(void) {
    sqrt(u + v) * pow(w, 2);
    printf("a. √(u + v) * w **2 = %f");
}

/* b. log(e) (x ** y) */
log(pow(x,y);

/* c. √((x-y) ** 3) */

/* d. |xy - w/z | */


